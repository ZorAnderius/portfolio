import styles from "./Logo.module.css";

const Logo = () => {
  return (
      <h2 className={styles.logo} aria-label="logo">
      _IVAN<span>OSTROVKA_</span>
    </h2>
  );
};

export default Logo;
